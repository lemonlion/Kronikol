diff --git a/src/main/java/net/sourceforge/plantuml/regex/Matcher2.java b/src/main/java/net/sourceforge/plantuml/regex/Matcher2.java
index 49ddb7a..e5b1a87 100644
--- a/src/main/java/net/sourceforge/plantuml/regex/Matcher2.java
+++ b/src/main/java/net/sourceforge/plantuml/regex/Matcher2.java
@@ -54,7 +54,7 @@ public class Matcher2 {
 	}
 
 	public static Matcher2 build(Pattern pattern, CharSequence input, int pos) {
-		final long now = System.currentTimeMillis();
+		final long now = INSTRUMENT ? System.currentTimeMillis() : 0;
 		final String id = pattern.pattern();
 		try {
 			final Matcher matcher = pattern.matcher(input);
@@ -69,6 +69,9 @@ public class Matcher2 {
 	}
 
 	public boolean matches() {
+		if (INSTRUMENT == false)
+			return matcher.matches();
+
 		final long now = System.currentTimeMillis();
 		try {
 			return matcher.matches();
@@ -119,6 +122,9 @@ public class Matcher2 {
 	}
 
 	public String group(int n) {
+		if (INSTRUMENT == false)
+			return matcher.group(n);
+
 		final long now = System.currentTimeMillis();
 		try {
 			return matcher.group(n);
@@ -128,6 +134,9 @@ public class Matcher2 {
 	}
 
 	public String group() {
+		if (INSTRUMENT == false)
+			return matcher.group();
+
 		final long now = System.currentTimeMillis();
 		try {
 			return matcher.group();
@@ -137,6 +146,9 @@ public class Matcher2 {
 	}
 
 	public int groupCount() {
+		if (INSTRUMENT == false)
+			return matcher.groupCount();
+
 		final long now = System.currentTimeMillis();
 		try {
 			return matcher.groupCount();
@@ -146,6 +158,9 @@ public class Matcher2 {
 	}
 
 	public boolean find() {
+		if (INSTRUMENT == false)
+			return matcher.find();
+
 		final long now = System.currentTimeMillis();
 		try {
 			return matcher.find();
@@ -155,6 +170,9 @@ public class Matcher2 {
 	}
 
 	public int end() {
+		if (INSTRUMENT == false)
+			return matcher.end();
+
 		final long now = System.currentTimeMillis();
 		try {
 			return matcher.end();
@@ -164,6 +182,9 @@ public class Matcher2 {
 	}
 
 	public int start() {
+		if (INSTRUMENT == false)
+			return matcher.start();
+
 		final long now = System.currentTimeMillis();
 		try {
 			return matcher.start();
